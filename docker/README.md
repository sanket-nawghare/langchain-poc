# Docker Configuration

Service-specific Docker configuration belongs here. The root `compose.yaml`
will be added during Phase 0.4 after the available Weaviate image and the HAPI
FHIR baseline have been confirmed.

