# Frontend

The frontend is a React, Vite, and TypeScript application. From this directory:

```bash
pnpm install
pnpm dev
```

Copy `.env.example` to `.env` if the backend is not available at the default
local URL.

Run all frontend formatting, linting, typing, and test checks from the
repository root:

```bash
make frontend-check
```
